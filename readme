蚂蚁课堂责任链模式，代码笔记
1.代码中例子是简单的使用不同的handler，new出来，然后设置下一个节点的方式。
2.还有一种数据库配置的形式，见doc文档，上一个节点，配置下一个节点的形式。
    2.1都是交给spring管理，由spring注入，getBean方式拿到handler。
    2.2数据库配置的好处是，可以自由组合，而不需要改动代码。自由组合哪个是first，second...
3.数据库的代码没有写，如果需要的话，可以配置上。
4.配上数据库，springboot完整的小项目了。需要的话自己配置上，改动代码，跑一跑。